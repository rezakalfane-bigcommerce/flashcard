"use client";

export function ArchiveConfirmation({ archived, phrase, pending, onCancel, onConfirm }: { archived: boolean; phrase: string; pending: boolean; onCancel: () => void; onConfirm: () => void }) {
  const action = archived ? "Un-archive" : "Archive";
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#15292d]/60 p-4 backdrop-blur-sm" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget && !pending) onCancel(); }}><div className="w-full max-w-md rounded-[2rem] border border-[#1d4d58]/15 bg-[#f4f8f7] p-7 text-[#15292d] shadow-2xl" role="dialog" aria-modal="true" aria-labelledby="archive-confirmation-title"><p className="mono text-[10px] uppercase tracking-[.2em] text-[#78979c]">{archived ? "Restore expression" : "Move expression"}</p><h2 id="archive-confirmation-title" className="display mt-2 text-4xl">{action} this expression?</h2><p className="mt-4 rounded-xl bg-white px-4 py-3 font-semibold text-[#1d4d58]">{phrase}</p><p className="mt-4 text-sm leading-6 text-[#52747a]">{archived ? "It will return to the active expression list and become available for study." : "It will leave the active list and no longer appear in flash cards, quizzes, or statistics."}</p><div className="mt-7 flex justify-end gap-3 border-t border-[#1d4d58]/10 pt-5"><button type="button" onClick={onCancel} disabled={pending} className="rounded-xl px-4 py-2 text-sm font-semibold text-[#52747a] hover:bg-[#d9eeec] disabled:opacity-40">Cancel</button><button type="button" onClick={onConfirm} disabled={pending} className={`rounded-xl px-5 py-2 text-sm font-semibold disabled:opacity-50 ${archived ? "bg-[#b7d86a] text-[#15292d] hover:bg-[#c6e57c]" : "bg-[#15292d] text-white hover:bg-[#1d4d58]"}`}>{pending ? "Saving…" : action}</button></div></div></div>;
}
